import 'package:dash/helpers/colors.dart';
import 'package:dash/screens/widgets/customButton.dart';
import 'package:flutter/material.dart';

class AccountSettingsModal extends StatelessWidget {
  const AccountSettingsModal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    //Bottom modal sheet with account settings
    return Container(
      padding: const EdgeInsets.all(15),
      child: Column(
        mainAxisSize: MainAxisSize.min,
        children: [

        ],
      ),
    );
  }
}
